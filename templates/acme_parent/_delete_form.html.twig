<form method="post" action="{{ path('acme_parent_delete', {'id': acme_parent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acme_parent.id) }}">
    <button class="btn">Delete</button>
</form>