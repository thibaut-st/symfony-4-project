{% extends 'base.html.twig' %}

{% block title %}Edit AcmeParent{% endblock %}

{% block body %}
    <h1>
        Edit AcmeParent
        <span class="btn-group float-right">
            <a class="btn btn-lg btn-info" href="{{ path('acme_parent_show', {'id': acme_parent.id}) }}">show</a>
            <a class="btn btn-lg btn-primary" href="{{ path('acme_parent_index') }}">back to list</a>
        </span>
    </h1>
    <hr>

    {{ include('acme_parent/_form.html.twig', {'button_label': 'Update'}) }}

    <hr>

    {{ include('acme_parent/_delete_form.html.twig') }}
{% endblock %}