{% extends 'base.html.twig' %}

{% block title %}Acme{% endblock %}

{% block body %}
    <h1>Acme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acme.id }}</td>
            </tr>
            <tr>
                <th>FieldA</th>
                <td>{{ acme.fieldA }}</td>
            </tr>
            <tr>
                <th>FieldB</th>
                <td>{{ acme.fieldB }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acme_index') }}">back to list</a>

    <a href="{{ path('acme_edit', {'id': acme.id}) }}">edit</a>

    {{ include('acme/_delete_form.html.twig') }}
{% endblock %}